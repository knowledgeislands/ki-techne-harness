name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: Exact release tag (for example v0.1.0)
        required: true
        type: string

permissions:
  contents: read

jobs:
  validate:
    name: Validate requested release
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.release.outputs.version }}
      tag-commit: ${{ steps.release.outputs.tag-commit }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 0
          persist-credentials: false
      - uses: jdx/mise-action@9e7f7633ff6f6d6048a9418a68d48f288f50eb14 # v4.2.3
      - run: bun install --frozen-lockfile
      - run: bun run test
      - id: release
        env:
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          REQUESTED_VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          [[ "$REQUESTED_VERSION" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]
          git fetch --no-tags origin "refs/tags/$REQUESTED_VERSION:refs/tags/$REQUESTED_VERSION"
          tag_commit=$(git rev-parse --verify "$REQUESTED_VERSION^{commit}")
          package_version=$(git show "$tag_commit:apps/cli/package.json" | bun -e \
            'let source=""; for await (const chunk of Bun.stdin.stream()) source += new TextDecoder().decode(chunk); console.log(JSON.parse(source).version)')
          test "$REQUESTED_VERSION" = "v$package_version"
          git merge-base --is-ancestor "$tag_commit" "origin/$DEFAULT_BRANCH"
          printf 'version=%s\n' "$REQUESTED_VERSION" >> "$GITHUB_OUTPUT"
          printf 'tag-commit=%s\n' "$tag_commit" >> "$GITHUB_OUTPUT"

  package:
    name: Package ${{ matrix.asset-target }}
    needs: validate
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            bun-target: bun-darwin-arm64
            asset-target: darwin-arm64
          - os: macos-latest
            bun-target: bun-darwin-x64
            asset-target: darwin-x64
          - os: ubuntu-latest
            bun-target: bun-linux-x64
            asset-target: linux-x64
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ needs.validate.outputs.tag-commit }}
          persist-credentials: false
      - uses: jdx/mise-action@9e7f7633ff6f6d6048a9418a68d48f288f50eb14 # v4.2.3
      - run: bun install --frozen-lockfile
      - run: bash release/package.sh "${{ needs.validate.outputs.version }}" "${{ matrix.bun-target }}" "${{ matrix.asset-target }}"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ matrix.asset-target }}
          path: dist/release/techne-${{ needs.validate.outputs.version }}-${{ matrix.asset-target }}.tar.gz
          if-no-files-found: error

  publish:
    name: Publish ${{ needs.validate.outputs.version }}
    needs: [validate, package]
    runs-on: ubuntu-latest
    environment: release
    permissions:
      actions: read
      contents: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: dist/release-assets
          merge-multiple: true
      - name: Create and verify release
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ needs.validate.outputs.version }}
        run: |
          set -euo pipefail
          cd dist/release-assets
          test "$(find . -maxdepth 1 -type f -name 'techne-*.tar.gz' | wc -l | tr -d ' ')" = 3
          sha256sum techne-"$VERSION"-darwin-arm64.tar.gz \
            techne-"$VERSION"-darwin-x64.tar.gz \
            techne-"$VERSION"-linux-x64.tar.gz > techne-checksums.txt
          sha256sum --check --strict techne-checksums.txt
          gh release create "$VERSION" --repo "$GITHUB_REPOSITORY" --verify-tag --draft \
            --title "$VERSION" --generate-notes techne-*.tar.gz techne-checksums.txt
          gh release edit "$VERSION" --repo "$GITHUB_REPOSITORY" --draft=false --latest
